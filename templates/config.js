// Placeholder deployment origin for this proposal (NOT the live lawrealestate.co.za
// domain — this is an independent redesign concept). Update SITE_URL once the
// proposal is actually deployed somewhere; canonical/OG tags are generated from it.
const SITE_URL = "https://law-real-estate-proposal.example";

module.exports = { SITE_URL };
