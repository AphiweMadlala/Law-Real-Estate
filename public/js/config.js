// Mirror of templates/config.js's BASE_PATH for browser-side scripts (kept in
// sync manually — this project has no bundler). See that file for why it exists.
export const BASE_PATH = "/Law-Real-Estate";
